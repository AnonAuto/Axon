# set the hostname 
HOSTNAME='taser-etmv2'
export HOSTNAME

# set the timezone
TZ='UTC'
export TZ

# set the format of the shell prompt
PS1='axonssh@\h:\w\$ '
export PS1

# ls aliases
alias ll='ls -lF'
alias la='ls -A'
